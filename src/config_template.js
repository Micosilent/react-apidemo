/**This is a template to store the API key from rawg.io, you can get yours from https://rawg.io/apidocs,
 *  replace it in this document, and rename it to config.js */
export let config = { APIKEY: "REPLACE_WITH_YOUR_APIKEY" };
